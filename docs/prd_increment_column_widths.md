# 增量 PRD：左侧表格列宽可调 + 负责人下拉按最长候选撑开

| 项 | 值 |
| --- | --- |
| Language | 中文（与历史 PRD 一致） |
| Project Name | web_gantt_planner |
| 增量版本 | v1.2（基线：v1.1 `prd_increment_roster_assignee.md` 已交付） |
| 编程语言/栈 | Vite + React + MUI + Tailwind（沿用，不变） |
| 文档性质 | **增量 PRD —— 仅描述变更部分**，未提及项一律视为保持现状 |

### 变更记录

| 日期 | 变更 | 说明 |
| --- | --- | --- |
| 2026-09-03 | 新增 **表格列宽可调**（拖动表头分隔条 + 双击按内容自适应 + localStorage 持久化） | 列定义集中在 `src/columns.ts`；拖期间只改 CSS 变量不触发 React 重渲染 |
| 2026-09-03 | **负责人下拉浮层按最长候选自动撑开**（max-content + 200/520 上下限） | MUI `slotProps.popper.style` 覆盖默认 `width` |

---

## 1. 增量需求

### 1.1 表格列宽可调（用户原始反馈）

> 用户原文：「让我们可以调整左边部分的列宽，现在有些长字符串无法完整显示。另外负责人列中弹出的清单过于狭窄，让我们更具最长字符调整宽度」

**两条独立诉求**：

1. **列宽可调**：表头每列右缘提供一条 7px 命中区的分隔条，鼠标拖动即改变列宽；最小/最大宽度按列类型夹紧（如任务名 120–960px、依赖 80–360px）。
2. **负责人下拉过窄**：MUI `Autocomplete` 默认把下拉浮层宽度锁成「与输入框同宽」，列宽 120px 时下拉只剩一条缝。改为按「最长候选文本宽度」自动撑开，配 200/520 上下限。

### 1.2 不在本次范围

- 不入 `Plan` 数据（列宽是纯 UI 偏好，不参与版本/锁/协同）
- 不动现有 8 列结构与列序
- 不改甘特图渲染

---

## 2. 行为细则

### 2.1 列宽调整（P0-1）

| 行为 | 期望 |
| --- | --- |
| 拖动表头分隔条 | 实时跟随，拖完后列宽写回 state |
| 越界（拖过 min/max） | 立即夹到边界，不留半截动作 |
| 双击分隔条 | 按该列当前可见内容（含缩进/折叠三角/日历按钮等占位）的最长一行重算宽度 |
| 调整完毕 | 立即写 localStorage，刷新页面后保留 |
| 极窄可视区 | 列宽之和超过容器 → 表格 `min-width` 触发横向滚动条；任务名称列用 `minmax(W, 1fr)` 吸收剩余空间 |

### 2.2 负责人下拉宽度（P0-2）

| 场景 | 浮层宽度 |
| --- | --- |
| 候选里最长 10 字符，列宽 120px | **200 px**（下限兜底） |
| 候选里最长 10 字符，列宽 90px | **200 px**（仍保下限） |
| 候选里最长 32 字符，列宽 360px | **按最长候选撑开**，封顶 520 px |

### 2.3 自适应不支持的列（P0-3）

「操作」列无文本内容，**不参与双击自适应**；双击该列分隔条回落到默认 136px。其他列均有自适应。

---

## 3. 验收清单（P0）

| 编号 | 验收项 |
| --- | --- |
| AC-1 | 表头有 7 条分隔条（8 列 → 7 条间隔） |
| AC-2 | 拖动任务名称分隔条 +100px，列宽与 `--col-name` CSS 变量同步变化 |
| AC-3 | 反向狂拖 -2000px，最终稳定在 min=120 |
| AC-4 | 正向狂拖 +2000px，最终稳定在 max=960 |
| AC-5 | 双击「任务名称」分隔条，按最长可见内容自适应（Project-A 计划 32 字符任务名 → 260 → 282） |
| AC-6 | 双击「依赖」分隔条，Project-A 计划从 110 → 210（按真实依赖表达式撑开） |
| AC-7 | 刷新页面后任意列宽保留（读 localStorage） |
| AC-8 | 负责人列宽 90/120/327 三档下，下拉浮层宽度均为 200 px（max-content 不超 200 因为最长候选 10 字） |
| AC-9 | 拖动期间 `body` 类加 `pg-resizing`，全局 `cursor: col-resize !important` + `user-select: none` |
| AC-10 | 单测 `src/__tests__/columns.test.ts` 覆盖 COLUMNS/clamp/grid/CSS 变量/load+save/autoFit 降级，全部通过 |
| AC-11 | 全部既有 252 个单测仍然通过，`tsc --noEmit` 0 错误 |