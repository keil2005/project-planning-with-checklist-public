node_modules/
dist/
data/
.workbuddy/
*.log
.DS_Store

# MPXJ 桥接（仅本机 Java 环境；Windows 零配置共享包不含此目录，导入自动 501）
server/mpxj/lib/
server/mpxj/*.class

# 本地专属配置（Mac 开发机开启自动同步；不进部署、不入库）
config/app.config.local.json

# 内网专属部署材料（共享盘启动器 / 内网主机名 / SMB 路径等；公开仓库不入）
deploy/internal/

# 跑测试时临时产生的导入备份目录
.test-imports-backup-*/

# CI 临时文件
.workbuddy/
